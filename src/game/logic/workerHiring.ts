export function hireWorker(cost:number,wallet:number,hired:boolean,prerequisite=true){if(hired)return{hired,wallet,changed:false,reason:"already-hired" as const};if(!prerequisite)return{hired,wallet,changed:false,reason:"prerequisite" as const};if(wallet<cost)return{hired,wallet,changed:false,reason:"insufficient-coins" as const};return{hired:true,wallet:wallet-cost,changed:true};}

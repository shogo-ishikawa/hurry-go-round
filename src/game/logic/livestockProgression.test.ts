import {describe,expect,it} from "vitest";import {getCoopParameters,getPoultryTargets,produceEggBatch,upgradeCoop} from "./livestock";
describe("coop progression",()=>{it("uses exact levels and prices",()=>{const base={feed:12,feedCapacity:12,eggs:0,eggCapacity:12};const two=upgradeCoop(1320,1,base);expect(two).toMatchObject({changed:true,wallet:900,level:2,livestock:{feedCapacity:18,eggCapacity:18}});const three=upgradeCoop(two.wallet,2,two.livestock);expect(three).toMatchObject({changed:true,wallet:0,level:3,livestock:{feedCapacity:24,eggCapacity:24}});expect(upgradeCoop(99,3,three.livestock)).toMatchObject({changed:false,wallet:99});expect(([1,2,3] as const).map(getCoopParameters)).toMatchObject([{chickens:3,eggBatch:1},{chickens:5,eggBatch:2},{chickens:7,eggBatch:3}]);});it("produces only feed/storage-safe partial batches",()=>{expect(produceEggBatch({feed:2,feedCapacity:24,eggs:23,eggCapacity:24},3)).toMatchObject({produced:1,livestock:{feed:1,eggs:24}});expect(getPoultryTargets(3)).toEqual({feedTarget:20,emergencyThreshold:7});});});

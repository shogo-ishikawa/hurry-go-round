import { describe, expect, it } from "vitest";
import { addCargoOne, createCarriedCargo, getCarriedTotal, removeCargoOne, unloadCargoToBarnBatch, unloadNextCargoOne } from "./resources";
describe("mixed cargo", () => {
  it("coexists and shares capacity", () => { let cargo=createCarriedCargo(12); cargo=addCargoOne(cargo,"wheat").cargo; cargo=addCargoOne(cargo,"corn").cargo; cargo=addCargoOne(cargo,"egg").cargo; expect(cargo.amounts).toEqual({wheat:1,corn:1,egg:1,flour:0,cornmeal:0,bread:0,cornbread:0,hay:0,milk:0,butter:0,cheese:0}); expect(getCarriedTotal(cargo)).toBe(3); });
  it("preserves unrelated resources and unloads round-robin",()=>{ const cargo={amounts:{wheat:2,corn:1,egg:1,flour:0,cornmeal:0,bread:0,cornbread:0,hay:0,milk:0,butter:0,cheese:0},capacity:12}; expect(removeCargoOne(cargo,"corn").cargo.amounts).toEqual({wheat:2,corn:0,egg:1,flour:0,cornmeal:0,bread:0,cornbread:0,hay:0,milk:0,butter:0,cheese:0}); const first=unloadNextCargoOne(cargo,{wheat:0,corn:0,egg:0,flour:0,cornmeal:0,bread:0,cornbread:0,hay:0,milk:0,butter:0,cheese:0},"wheat"); expect(first.resource).toBe("corn"); const second=unloadNextCargoOne(first.cargo,first.destination,first.resource); expect(second.resource).toBe("egg"); });
  it("supports every upgrade capacity without overflow",()=>{for(const capacity of [12,18,24]){let cargo=createCarriedCargo(capacity);for(let i=0;i<capacity+1;i++)cargo=addCargoOne(cargo,"wheat").cargo;expect(getCarriedTotal(cargo)).toBe(capacity);}});
  it("atomically unloads all eleven resources in stable order and conserves them",()=>{const cargo=createCarriedCargo(99);for(const [index,id] of (["wheat","corn","egg","flour","cornmeal","bread","cornbread","hay","milk","butter","cheese"] as const).entries())cargo.amounts[id]=index+1;const barn={...cargo.amounts};const before=getCarriedTotal(cargo)+Object.values(barn).reduce((sum,value)=>sum+value,0);const result=unloadCargoToBarnBatch(cargo,barn);expect(result.breakdown.map(item=>item.resource)).toEqual(["wheat","corn","egg","flour","cornmeal","bread","cornbread","hay","milk","butter","cheese"]);expect(result.totalMoved).toBe(66);expect(getCarriedTotal(result.cargo)).toBe(0);expect(Object.values(result.barn).reduce((sum,value)=>sum+value,0)).toBe(before);expect(unloadCargoToBarnBatch(result.cargo,result.barn).changed).toBe(false);});
});

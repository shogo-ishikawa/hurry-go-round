import {expect,test,type Page} from "@playwright/test";
const names=["麦","とうもろこし","たまご","小麦粉","コーンミール","パン","コーンブレッド","干し草","牛乳","バター","チーズ"];
async function ready(page:Page){const errors:string[]=[];page.on("pageerror",error=>errors.push(error.message));page.on("console",message=>{if(message.type()==="error")errors.push(message.text());});await page.goto("");await page.getByRole("button",{name:/はじめる/}).click();await expect.poll(()=>page.evaluate(()=>window.__HGR_E2E__?.isGameReady())).toBe(true);return errors;}
test("ledger shows exact carried and warehouse rows, paginates, and survives reload",async({page})=>{const errors=await ready(page),values=Object.fromEntries(["wheat","corn","egg","flour","cornmeal","bread","cornbread","hay","milk","butter","cheese"].map((id,index)=>[id,index+1]));await page.evaluate(value=>window.__HGR_E2E__!.configureInventory({cargo:value,cargoCapacity:100,barn:Object.fromEntries(Object.entries(value).map(([id,n])=>[id,n+11]))}),values);const hud=await page.evaluate(()=>window.__HGR_E2E__!.getInventoryDiagnostics());expect(hud.carried).toContain("一覧");expect(hud.barn).toContain("一覧");await page.keyboard.press("i");let panel=await page.evaluate(()=>window.__HGR_E2E__!.getInventoryDiagnostics());expect(panel.section).toBe("持ち物・倉庫");expect(panel.rows.map(row=>row.name)).toEqual(names);panel.rows.forEach((row,index)=>{expect(row.carried).toBe(index+1);expect(row.barn).toBe(index+12);expect(row.availableForContract).toBe(index*2+13);});expect(panel.buttons.every(button=>button.x>=0&&button.x+button.width<=page.viewportSize()!.width)).toBe(true);await page.keyboard.press("PageDown");panel=await page.evaluate(()=>window.__HGR_E2E__!.getInventoryDiagnostics());expect(panel.scrollPosition).toBeGreaterThanOrEqual(0);await page.keyboard.press("Escape");await page.evaluate(()=>window.__HGR_E2E__!.requestSave());await page.reload();await page.getByRole("button",{name:"つづきから"}).click();await expect.poll(()=>page.evaluate(()=>window.__HGR_E2E__?.isGameReady())).toBe(true);await page.keyboard.press("i");panel=await page.evaluate(()=>window.__HGR_E2E__!.getInventoryDiagnostics());expect(panel.rows[10]).toMatchObject({carried:11,barn:22});expect(errors).toEqual([]);});
for(const viewport of [{width:1920,height:1080},{width:1440,height:900},{width:844,height:390},{width:390,height:844},{width:320,height:568}])test(`processing overview is visibly reachable at ${viewport.width}x${viewport.height}`,async({page})=>{await page.setViewportSize(viewport);const errors=await ready(page);await page.evaluate(()=>window.__HGR_E2E__!.openProcessingPanel(0));let panel=await page.evaluate(()=>window.__HGR_E2E__!.getProcessingPanel());expect(panel.pageName).toBe("概要");expect(panel.visibleText.some(item=>item.visible&&!item.clipped&&item.text.includes("加工場では"))).toBe(true);const reached=new Set<string>();for(let pageIndex=0;pageIndex<8;pageIndex++){panel=await page.evaluate(()=>window.__HGR_E2E__!.getProcessingPanel());panel.visibleText.filter(item=>item.visible&&!item.clipped).forEach(item=>reached.add(item.text));if(!panel.buttons.some(button=>button.label==="次へ"&&button.enabled))break;await page.mouse.wheel(0,120);}const all=[...reached].join(" ");expect(all).toContain("麦 2 → 小麦粉 1");expect(all).toContain("コーンブレッド 1");expect(panel.buttons.every(button=>button.x>=-1&&button.x+button.width<=viewport.width+1&&button.y>=-1&&button.y+button.height<=viewport.height+1)).toBe(true);await page.keyboard.press("Tab");await page.keyboard.press("Shift+Tab");await page.keyboard.press("Escape");expect(errors).toEqual([]);});
